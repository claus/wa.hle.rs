---
// Resets, global styles
import '@/styles/global/reset.scss';
import '@/styles/global/theme.scss';

// Global CSS variable definitions
import '@/styles/global/grid.scss';
import '@/styles/global/colors.scss';
import '@/styles/global/animations.scss';
import '@/styles/global/misc.scss';

// Global text styles
import '@/styles/global/typography.scss';

import { ViewTransitions } from 'astro:transitions';

import Header from '@/components/ui/Header/Header.astro';
import ThemeInit from '@/components/ui/Theme/ThemeInit.astro';
import GridOverlay from '@/components/ui/GridOverlay/GridOverlay.astro';

import styles from './Layout.module.scss';

interface Props {
    title?: string;
}

const { title = 'Claus Wahlers' } = Astro.props;
---

<html lang="en">
    <head>
        <meta charset="utf-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
        <meta name="title" content={title} />
        <meta name="description" content="Claus Wahlers" />
        <meta name="generator" content={Astro.generator} />
        <link
            rel="alternate"
            type="application/rss+xml"
            title="wa.hle.rs"
            href={`${Astro.site}/rss.xml`}
        />
        <link
            rel="preload"
            href="/assets/fonts/volume_tc_regular-webfont.woff2"
            as="font"
            type="font/woff2"
            crossorigin="true"
        />
        <link
            rel="preload"
            href="/assets/fonts/volume_tc_sans-webfont.woff2"
            as="font"
            type="font/woff2"
            crossorigin="true"
        />
        <link rel="sitemap" href="/sitemap-index.xml" />
        <link rel="icon" href="/favicon.ico" sizes="any" />
        <link rel="icon" href="/favicon.svg" type="image/svg+xml" />
        <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
        <link rel="manifest" href="/manifest.webmanifest" />
        <title>{title}</title>
        <ThemeInit />
        <ViewTransitions />
    </head>
    <body>
        <div class:list={[styles.main, 'body']}>
            <slot />
        </div>
        <GridOverlay />
    </body>
</html>
